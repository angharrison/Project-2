<section class="profile">
  <h2>{{user.local.email}}</h2>
  {{#if user.scents}}
    <div class="scents">
      {{#each user.scents}}
         {{> 'scent'}}
      {{/each}}
    </div>
  {{else}}
    <p>No User yet</p>
  {{/if}}
</section>
